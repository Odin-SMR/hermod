2006-08-09 Bugfixes of last change <joakim.moller@chalmers.se>

	* l1b/insertFiles.py,l1b/transitions.py: a lot of small bugs
	corrected.

2006-08-08 Objectorieted insertFiles<joakim.moller@chalmers.se>

	* l1b/insertFiles.py: Total remake of structural design.

	* l1b/transitions.py: Object that handles SMR-structure and submitting
	jobs to queue.

2006-08-02 Objectoriented readFreq <joakim.moller@chalmers.se>

	* l2/readFreq.py: Total remake of structural design - towards making
	it easy to reuse code or use it in the python interpreter.

	* l2/collectStd.py: removed, not needed anymore

2006-08-01 Major changes <joakim.moller@chalmers.se>
	
	* Almost all files: fqid is a new parameter in level2 table. Major
	parts of the sourcecode was rewritten. No fundamental changes in
	functionality

	* l1b/insertFiles.py: a compiled matlab script runs instead of
	create_tp

	* l1b/rerunOrbits.py: changed in-parameter. see usage()

	
2005-10-19 Error to stderr <joakim.moller@chalmers.se>

	* l1b/insertFiles.py: Moving files to a spool_failure director i files not
	having more than 3 scan. Also errors are written to stderr.
	
2005-10-06 Bad orbit files <joakim.moller@chalmers.se>

	* l1b/insertFiles.py: Added a check to find bad orbit files, files
	with less than 3 scans are ignored.

	* l1b/insert_level1b.awk, l2/test, l2/update.log,
	l2/update_l2_files.awk, l2/update_level2_files.awk: removed, not used anymore.
2005-09-19 Freqmode search <joakim.moller@chalmers.se>

	* l2/readFreq.py: Problems to read 3digit hexorbit number fixed

	* l1b/rerunOrbits.py: Fixed and operational, but not safe.

	* l1b/insertFiles.py: Fixed problem with 3digits.

2005-09-19 Freqmode search <joakim.moller@chalmers.se>

	* l1b/rerunOrbit.py: Added a inparameter to make it easier to search
	for not processed data.

2005-09-19 Three digit bugfix <joakim.moller@chalmers.se>

	* l1b/readFreq.py: Adds zeros in front of the orbitnumber if it has
	less than 4 digits. 

2005-09-16 Some bugfixes and cleanup <joakim.moller@chalmers.se>

	* l1b/read_hdf.c: Compilation instructions in file

	* l1b/inserFiles.py: Some very verbose printouts removed

	* l1b/msl.awk, l1b/freqmodes, l1b/freqmodes_nr, l1b/freqmodes_str:
	removed.

	* l1b/rerunOrbits.py: Tool to rerun orbit files. Note this is program
	is not tested yet. Use were carefully! Talk to Joakim first.

2005-08-29 Pythonised startscript <joakim.moller@chalmers.se>

	* l1b/insertFiles.py: Problems with different kinds of calibration
	solved. Now '3' is the one and only calibration spectrum.

2005-08-25 Pythonised startscript <joakim.moller@chalmers.se>
	
	* l1b/insertFiles.py: Inserts all files in spooldir into odin
	database, reorganises files according to SMR-directory structure.

2005-07-18 Joakim Möller <joakim.moller@chalmers.se>
	
	* l1b/msl.awk: Changing directory to ~logs/ before sending job to
	queue. This to avoid logfiles in odinops home directory.

2005-07-15 Hermod in cvs <joakim.moller@chalmers.se>

	* files-pdc/: Scripts that fetches files from pdc

	* files-nilu/: Scripts that fetches files from nilu

	* l1b/: gawk-scripts, adds l1b to the database and starts processings

	* l2b/: python-scripts, adds l2-files to the database.
